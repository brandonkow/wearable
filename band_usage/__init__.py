"""band_usage — show Claude Code & Codex usage on a Samsung Galaxy Fit 3.

The Fit 3 runs proprietary firmware with no public SDK, so it cannot run
custom apps or watch faces. What it *can* do is mirror notifications from the
paired phone. This package computes your local AI CLI usage and pushes a
compact notification (via ntfy) that shows up as a glanceable card on the band.
"""

__version__ = "0.1.0"
